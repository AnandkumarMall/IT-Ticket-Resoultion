# ============================================================
# Backend Environment Variables
# Copy this file to .env and fill in your actual values.
# ============================================================

# --- Database (leave blank to use local SQLite for development) ---
# On cloud, set this to your PostgreSQL connection string.
# Heroku/Render Postgres example:
DATABASE_URL=postgresql://user:password@host:5432/dbname

# --- CORS (comma-separated list of allowed frontend origins) ---
# Leave blank locally (defaults to * which allows all origins).
# On cloud, restrict to your Flask frontend URL:
ALLOWED_ORIGINS=https://your-flask-app.onrender.com

# ============================================================
# IT Ticket Resolution Suggestion Engine — Backend Dependencies
# ============================================================

# Web framework
fastapi==0.110.0
uvicorn[standard]==0.29.0

# Data validation
pydantic[email]==2.6.4

# NLP / ML (TF-IDF + cosine similarity)
scikit-learn==1.4.1.post1
pandas==2.2.1
nltk==3.8.1

# Security — password hashing
bcrypt==4.1.2

# Cloud database — PostgreSQL support (used when DATABASE_URL env var is set)
# Not needed for local SQLite development.
psycopg2-binary==2.9.9
